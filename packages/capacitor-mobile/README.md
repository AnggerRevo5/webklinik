# Capacitor Mobile

Capacitor wrapper untuk menjalankan admin-dashboard sebagai mobile application (iOS/Android).

## Struktur

- Capacitor configuration
- Native plugins setup
- Platform-specific builds

## Setup

```bash
cd packages/capacitor-mobile
npm install
npx cap add ios
npx cap add android
npx cap build ios
npx cap build android
```
